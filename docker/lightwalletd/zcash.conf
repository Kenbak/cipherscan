# lightwalletd -> zebrad RPC connection (inside the compose network).
# Zebra runs with cookie auth disabled on the internal network, so these
# credentials are placeholders required only by lightwalletd's config parser.
rpcbind=zebrad
rpcport=8232
rpcuser=cipherscan
rpcpassword=cipherscan
