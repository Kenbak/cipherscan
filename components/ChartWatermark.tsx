export type WatermarkSize = 'sm' | 'md' | 'lg' | 'map';

/** Kept as a compatibility slot. Attribution belongs outside the data area. */
export function ChartWatermark(_props: { className?: string; label?: string; size?: WatermarkSize }) {
  void _props;
  return null;
}
