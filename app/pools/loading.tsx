import { PageLoading } from '@/components/ui/PageLoading';

export default function Loading() {
  return <PageLoading layout="pools" title="Zcash Shielded Pools" eyebrow="POOL_ANALYTICS" subtitle="Supply held in shielded pools, its history, and the public flows moving ZEC in and out." />;
}
