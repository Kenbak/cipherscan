import { PageLoading } from '@/components/ui/PageLoading';

export default function Loading() {
  return <PageLoading layout="wallets" title="Zcash Wallet Signals" eyebrow="WALLET_ANALYSIS" subtitle="Fee patterns, observable wallet signals and their limits." />;
}
