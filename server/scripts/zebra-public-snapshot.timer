[Unit]
Description=Every 6h: publish Zebra Crosslink bootstrap snapshot

[Timer]
# Wait 30 minutes after boot so the node is well-synced first
OnBootSec=30min
OnUnitActiveSec=6h
RandomizedDelaySec=5min
Persistent=true

[Install]
WantedBy=timers.target
